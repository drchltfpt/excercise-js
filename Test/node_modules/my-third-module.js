// A variable
const COPY_RIGHT = "ha.phamhuy@vietis.com.vn";

// A function
const multiple = function(a, b) {

    return a * b;
};

// A class
const Person = class {

    constructor(name, age) {
        this.name = name;
        this.age = age;
    }

    showInfo() {
        console.log("name=" + this.name + ", age=" + this.name);
    }
};

// Something to Export
const somthingToExport = {
    Person: Person,
    multiple: multiple,
    COPY_RIGHT: COPY_RIGHT
};

// Export it!!
module.exports = somthingToExport;